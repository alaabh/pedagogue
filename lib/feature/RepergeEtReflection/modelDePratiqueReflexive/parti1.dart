import 'package:flutter/material.dart';

class parti1 extends StatelessWidget {
  const parti1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
