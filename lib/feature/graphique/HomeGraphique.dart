import 'package:flutter/material.dart';

class HomeGraphique extends StatefulWidget {
  const HomeGraphique({super.key});

  @override
  State<HomeGraphique> createState() => _HomeGraphiqueState();
}

class _HomeGraphiqueState extends State<HomeGraphique> {
  @override
  Widget build(BuildContext context) {
    return  SingleChildScrollView(
      child: Column(
        children: [

        ],
      ),
    );
  }
}

